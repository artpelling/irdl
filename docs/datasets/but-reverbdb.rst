BUT ReverbDB is distributed under provider-specific access and licensing terms. Obtain the RIR-only
archive from Speech@FIT and pass the archive or extracted directory as ``source_path``. IRDL does not
bypass those terms or embed an unstable HTTP URL. Only ``output_format="raw"`` is supported.
