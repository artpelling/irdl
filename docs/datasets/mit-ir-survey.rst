The MIT Impulse Response Survey aggregates responses collected from many rooms. Download the survey
from the provider page, then pass ``mit_audio.zip`` or its extracted ``Audio`` directory as
``source_path``. The provider page does not expose a stable versioned archive URL, so IRDL does not
automate acquisition. Only ``output_format="raw"`` is supported.
