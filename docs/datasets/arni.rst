Arni contains repeated room impulse responses for combinations of movable absorbing panels. Pass the
original WAV directory as ``source_path``. Automatic acquisition is intentionally unavailable because
the Zenodo record exposes a very large provider file set rather than one stable canonical archive.
Only ``output_format="raw"`` is supported; IRDL does not invent spatial coordinates.
