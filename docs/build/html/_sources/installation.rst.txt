Installation
============

Simply add

.. code-block:: toml

    dependencies = [
        "irdl @ git+https://github.com/artpelling/irdl"
    ]

to your ``pyproject.toml`` (or similarly to ``requirements.txt``).
